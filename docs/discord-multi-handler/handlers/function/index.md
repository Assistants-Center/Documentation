# Function Handler

## Creating the Handler
```js
const { FunctionHandler } = require('discord-multi-handler');
const functionHandler = new FunctionHandler(client);
```
### Client
The client is the discord.js client that you have created.